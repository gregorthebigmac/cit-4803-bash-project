#!/bin/bash

# this will check if the user has permission to see the directory contents before allowing them to change into the directory specified.
# In the backend, this runs:
# $ cd [path/to/dir]

echo "correctly called [cd]"
