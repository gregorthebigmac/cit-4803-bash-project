#!/bin/bash

source $root_dir/system_files/bin/sys_bin/error_report.sh

dir_list="$(ls --color --group-directories-first "$current_dir")"
for x in $dir_list; do
	if [[ "${x:0:1}" != "." ]]; then
		echo "$x"
	fi
done
