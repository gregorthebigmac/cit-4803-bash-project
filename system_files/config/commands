cd		| DONE
chgpass	| DONE
del 	| DONE
deluser	| we can fake it
edit	| hard part's done with "cd" check permissions. The user can't edit a file if they can't enter the directory
list	| DONE
mkfile	| hard part's done with "cd" check permissions. The user can't make a file if they can't enter the directory
mkusr	| we can fake it
move	| hard part's done with "cd" check permissions. The user can't move a file if they can't enter the directory
search	| 
errors	| 
