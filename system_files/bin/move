#!/bin/bash

# This will check if the user has permission to move the file from one directory to the other specified before executing the command in the BASH shell:
# $ mv [input/path/filename.ext] [output/path/filename.ext]
